# Simple Weather App

A weather forecast web application built with React, Javascript, SASS, the OpenWeatherMap API and Axios

## Key Features

- Allows the user to select between several predefined cities
- Allows refresh data
- It's connected with OpenWeatherMap API
